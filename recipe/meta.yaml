{% set name = "verible" %}
{% set version = "0.0-3702-g73f291dc" %}

package:
  name: {{ name|lower }}
  version: {{ version|replace("-", "_") }}

source:
  url: https://github.com/chipsalliance/verible/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 6f22191b5d2748e38517af8b6ebcacab3fa9a413ad98d0f38ee56ce64dc5b7f2
  patches:
    - build_opts_patch.patch


build:
  number: 0
  skip: true  # [win]

requirements:
  build:
    - bazel >=5,<=7
    - bazel-toolchain
    - flex
    - bison
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - patchelf

test:
  commands:
    - verible-verilog-lint --version
    - verible-verilog-diff --version

about:
  home: https://github.com/chipsalliance/verible
  summary: |
    The Verible project's main mission is to parse SystemVerilog (IEEE 1800-2017)
    (as standardized in the SV-LRM) for a wide variety of applications, including 
    developer tools.
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE

extra:
  recipe-maintainers:
    - dillonroach
